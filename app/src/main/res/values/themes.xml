<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Theme dùng cho Splash Screen -->
    <style name="Theme.DriverTest.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.DriverTest</item>
    </style>

    <!-- Theme chính của ứng dụng -->
    <style name="Base.Theme.DriverTest" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.DriverTest" parent="Base.Theme.DriverTest" />

</resources>